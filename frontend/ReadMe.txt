To Run Backend:

1. Go to backend directory
2. Activate virtual environment by "source .venv/bin/activate"
3. Start backend application by running the command "uvicorn main:app --reload"

To Run Frontend:

1. Go to frontend directory
2. Start backend application by running the command "npm run dev"